import { experience } from "@/data/experience";
export function ExperienceSection() { return <section id="experience" className="section-shell scroll-mt-16 border-t border-slate-800/80 py-20 sm:py-28"><p className="eyebrow">03 / Experience</p><h2 className="section-heading">Engineering in context.</h2><div className="mt-12 border-l border-slate-700 pl-6 sm:pl-10">{experience.map((entry) => <article className="relative pb-14 last:pb-0" key={entry.company}><span className="absolute -left-[31px] top-1 size-3 rounded-full border-2 border-[#080b12] bg-cyan-300 sm:-left-[47px]"/><div className="flex flex-wrap items-baseline justify-between gap-2"><div><h3 className="text-xl font-semibold text-white">{entry.company}</h3><p className="mt-1 text-sm text-cyan-100">{entry.role} <span className="text-slate-500">— {entry.type}</span></p></div><p className="font-mono text-xs text-slate-500">{entry.startDate} – {entry.endDate}</p></div><p className="mt-4 max-w-3xl text-sm leading-6 text-slate-400">{entry.description}</p><ul className="mt-4 grid max-w-4xl gap-2 sm:grid-cols-2">{entry.responsibilities.map((item) => <li className="text-sm text-slate-300" key={item}>— {item}</li>)}</ul><div className="mt-5 flex flex-wrap gap-1.5">{entry.technologies.map((tech) => <span className="rounded border border-slate-700 px-2 py-1 font-mono text-[10px] text-slate-500" key={tech}>{tech}</span>)}</div></article>)}</div></section>; }
