import { ArrowRight } from "lucide-react";
import { ProjectExplorer } from "@/components/projects/project-explorer";
import { featuredProjects, projects } from "@/data/projects";

export function ProjectsSection() { return <section id="projects" className="section-shell scroll-mt-16 border-t border-slate-800/80 py-20 sm:py-28"><p className="eyebrow">02 / Selected work</p><div className="flex flex-wrap items-end justify-between gap-4"><h2 className="section-heading">Built around the operating system.</h2><p className="max-w-md text-sm leading-6 text-slate-400">Cloud-native delivery, platform automation, security and observability — treated as one engineering system.</p></div><div className="mt-12 grid gap-4 lg:grid-cols-2">{featuredProjects.map((project, index) => <article className={`surface rounded-lg p-6 ${index === 0 ? "lg:col-span-2" : ""}`} key={project.slug}><div className="flex flex-wrap items-center justify-between gap-3"><span className="font-mono text-xs text-cyan-200">0{index + 1} / {project.categories.join(" · ")}</span><span className="text-xs text-slate-500">Featured project</span></div><div className="mt-7 grid gap-6 md:grid-cols-[1fr_.8fr]"><div><h3 className="text-2xl font-semibold tracking-tight text-white">{project.title}</h3><p className="mt-3 text-sm leading-6 text-slate-400">{project.description}</p><ul className="mt-5 space-y-2">{project.highlights.slice(0, 3).map((item) => <li className="flex gap-2 text-sm text-slate-300" key={item}><ArrowRight className="mt-0.5 shrink-0 text-cyan-300" size={14}/>{item}</li>)}</ul></div>{project.architecture && <div className="border-l border-slate-700 pl-5"><p className="font-mono text-[10px] tracking-wider text-slate-500 uppercase">Architecture</p><div className="mt-4 space-y-1">{project.architecture.nodes.map((node, nodeIndex) => <div className="flex items-center gap-3" key={node}><span className="font-mono text-[10px] text-cyan-400">{nodeIndex + 1}</span><span className="border border-slate-700 bg-slate-900/60 px-2 py-1 font-mono text-xs text-slate-300">{node}</span></div>)}</div></div>}</div><div className="mt-6 flex flex-wrap gap-1.5 border-t border-slate-800 pt-5">{project.technologies.map((tech) => <span className="rounded border border-slate-700 px-2 py-1 font-mono text-[10px] text-slate-400" key={tech}>{tech}</span>)}</div></article>)}</div><div className="mt-20"><p className="eyebrow">Project explorer</p><h3 className="mt-3 text-2xl font-semibold tracking-tight text-white">More engineering work</h3><ProjectExplorer projects={projects.filter((project) => !project.featured)} /></div></section>; }
