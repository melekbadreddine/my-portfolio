import { EngineeringMap } from "@/components/architecture/engineering-map";
import { certifications } from "@/data/certifications";
import { skillCategories } from "@/data/skills";
export function SkillsSection() { const completed = certifications.filter((c) => c.status === "completed"); const pursuing = certifications.filter((c) => c.status !== "completed"); return <section id="skills" className="section-shell scroll-mt-16 border-t border-slate-800/80 py-20 sm:py-28"><p className="eyebrow">04 / Tooling &amp; approach</p><h2 className="section-heading">How I engineer systems.</h2><EngineeringMap /><div className="mt-20 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{skillCategories.map((category) => <article className="surface rounded-lg p-5" key={category.name}><h3 className="text-sm font-semibold text-white">{category.name}</h3><div className="mt-4 flex flex-wrap gap-1.5">{category.skills.map((skill) => <span className="rounded border border-slate-700 px-2 py-1 font-mono text-[10px] text-slate-400" key={skill}>{skill}</span>)}</div></article>)}</div><div className="mt-20 grid gap-8 md:grid-cols-2"><div><p className="eyebrow">Credentials</p><h3 className="mt-3 text-xl font-semibold text-white">Completed</h3><ul className="mt-5 space-y-3">{completed.map((cert) => <li className="border-l border-cyan-300 pl-4 text-sm text-slate-300" key={cert.name}>{cert.name}</li>)}</ul></div><div><p className="eyebrow">Current journey</p><h3 className="mt-3 text-xl font-semibold text-white">Currently pursuing</h3><ul className="mt-5 space-y-3">{pursuing.map((cert) => <li className="border-l border-slate-600 pl-4" key={cert.name}><p className="text-sm text-slate-300">{cert.name}</p><p className="mt-1 text-sm leading-6 text-slate-500">{cert.description}</p></li>)}</ul></div></div></section>; }
